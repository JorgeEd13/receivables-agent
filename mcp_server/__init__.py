"""MCP (Model Context Protocol) server exposing the receivables ledger.

Named ``mcp_server`` (not ``mcp``) on purpose: a top-level ``mcp`` package would
shadow the MCP SDK's own ``mcp`` package on the path (ADR-008).
"""
