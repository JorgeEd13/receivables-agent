# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Env / secrets
.env
*.key

# Local-only CA bundle used to make `docker build` work on a network that
# terminates TLS (see Dockerfile.hf — the COPY is an optional glob).
# Never committed and never needed on HF's clean runners — a local dev artifact only.
ca.crt

# Generated data artifacts
data/*.duckdb
data/*.parquet
.chroma/
chroma/

# Node / web
web/node_modules/
web/dist/

# Local-only compose override (points the container at host Ollama; reference
# only — the image is Gemini-only, so it isn't a supported path). See STATE.md.
docker-compose.override.yml

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Blind-adversarial repros and throwaway probes — local only, never shipped.
scratchpad/
